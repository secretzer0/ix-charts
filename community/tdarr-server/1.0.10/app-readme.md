# Tdarr Server

[Tdarr](https://home.tdarr.io/) Server for Distributed Transcoding

- Server runs with optional internal node
- Web UI on port 30265, server port 30266
- Remote nodes must mount media at same paths: /media/Movies, /media/TV, /transcode/Movies, /transcode/TV, /temp
- App runs as `root` user
